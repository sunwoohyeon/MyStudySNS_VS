# 타임라인 조회 흐름도

## 1. 기능 개요

타임라인은 홈 화면에서 게시글 목록을 표시하고 필터링, 정렬하는 핵심 기능입니다.

### 주요 특징

- **해시태그 필터링**: 주제별 게시글 필터
- **정렬 옵션**: 최신순 / 유용순
- **무한 스크롤**: 페이지 단위 로딩
- **섹션 분리**: 최신 게시글, 게시판별 상위 게시글

---

## 2. 전체 흐름도

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                           타임라인 조회 전체 흐름                                    │
└─────────────────────────────────────────────────────────────────────────────────────┘

     사용자
        │
        │ 1. 홈 페이지 접속
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    page.tsx (홈 페이지)                                    │
│                                                                           │
│  return (                                                                 │
│    <MainLayout>                                                           │
│      <AuthGuard />                                                        │
│      <KnowledgeCards />      {/* 추천 지식카드 */}                       │
│      <ScheduleWidget />       {/* 내 시간표 */}                          │
│      <Timeline />             {/* 게시글 타임라인 */}                    │
│    </MainLayout>                                                          │
│  );                                                                       │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 2. Timeline 컴포넌트 마운트
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    Timeline.tsx - 초기 데이터 로딩                        │
│                                                                           │
│  // 상태 초기화                                                           │
│  const [sortBy, setSortBy] = useState<'latest' | 'useful'>('latest');    │
│  const [selectedTopic, setSelectedTopic] = useState("전체");             │
│  const [posts, setPosts] = useState<Post[]>([]);                         │
│  const [loading, setLoading] = useState(true);                           │
│  const [currentPage, setCurrentPage] = useState(1);                      │
│  const POSTS_PER_PAGE = 10;                                              │
│                                                                           │
│  useEffect(() => {                                                       │
│    fetchPosts();                                                          │
│  }, [sortBy]);  // 정렬 변경 시 재로딩                                   │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 3. 게시글 데이터 조회
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    Supabase 쿼리                                          │
│                                                                           │
│  const fetchPosts = async () => {                                        │
│    let query = supabase                                                   │
│      .from("posts")                                                       │
│      .select(`                                                            │
│        *,                                                                 │
│        profiles:user_id (                                                 │
│          username,                                                        │
│          school_name                                                      │
│        ),                                                                 │
│        post_hashtags (                                                    │
│          hashtag_id,                                                      │
│          hashtags (                                                       │
│            id,                                                            │
│            name                                                           │
│          )                                                                │
│        ),                                                                 │
│        reviews (                                                          │
│          score                                                            │
│        )                                                                  │
│      `);                                                                  │
│                                                                           │
│    // 정렬 적용                                                           │
│    if (sortBy === 'latest') {                                            │
│      query = query.order('created_at', { ascending: false });            │
│    }                                                                      │
│    // 유용순은 클라이언트에서 계산                                        │
│                                                                           │
│    const { data, error } = await query;                                  │
│    setPosts(data || []);                                                  │
│  };                                                                       │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 4. UI 렌더링
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    Timeline UI 구조                                       │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                                                                     │ │
│  │  📝 최신 게시글                       [최신순 ▼]  [전체 ▼]         │ │
│  │                                                                     │ │
│  │  ┌─────────────────────────────────────────────────────────────┐   │ │
│  │  │  TopicFilter 컴포넌트                                        │   │ │
│  │  │                                                             │   │ │
│  │  │  [전체] [#react] [#javascript] [#python] [#화학] [#수학]... │   │ │
│  │  │                                                             │   │ │
│  │  └─────────────────────────────────────────────────────────────┘   │ │
│  │                                                                     │ │
│  │  ┌─────────────────────────────────────────────────────────────┐   │ │
│  │  │  PostList 컴포넌트                                          │   │ │
│  │  │                                                             │   │ │
│  │  │  ┌─────────────────────────────────────────────────────┐   │   │ │
│  │  │  │  PostItem 1                                          │   │   │ │
│  │  │  │  [스터디노트] React Hooks 완벽 정리                   │   │   │ │
│  │  │  │  #react #hooks #frontend                             │   │   │ │
│  │  │  │  👍 24  💬 5  📅 2시간 전                             │   │   │ │
│  │  │  └─────────────────────────────────────────────────────┘   │   │ │
│  │  │                                                             │   │ │
│  │  │  ┌─────────────────────────────────────────────────────┐   │   │ │
│  │  │  │  PostItem 2                                          │   │   │ │
│  │  │  │  ...                                                 │   │   │ │
│  │  │  └─────────────────────────────────────────────────────┘   │   │ │
│  │  │                                                             │   │ │
│  │  └─────────────────────────────────────────────────────────────┘   │ │
│  │                                                                     │ │
│  │  [더 보기]                                                          │ │
│  │                                                                     │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 필터링 흐름

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                           해시태그 필터링 흐름                                       │
└─────────────────────────────────────────────────────────────────────────────────────┘

     사용자
        │
        │ 해시태그 클릭 (예: #react)
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    TopicFilter.tsx                                        │
│                                                                           │
│  // 해시태그 목록 조회                                                    │
│  useEffect(() => {                                                       │
│    const fetchHashtags = async () => {                                   │
│      const { data } = await supabase                                     │
│        .from("hashtags")                                                  │
│        .select("name, count")                                            │
│        .order("count", { ascending: false })                             │
│        .limit(20);                                                        │
│                                                                           │
│      setHashtags(data || []);                                            │
│    };                                                                     │
│    fetchHashtags();                                                       │
│  }, []);                                                                  │
│                                                                           │
│  // 해시태그 선택                                                         │
│  const handleSelect = (tag: string) => {                                 │
│    onSelectTopic(tag);  // 부모 컴포넌트로 전달                          │
│  };                                                                       │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    Timeline.tsx - 필터링 적용                             │
│                                                                           │
│  // 클라이언트 측 필터링 (useMemo)                                       │
│  const filteredPosts = useMemo(() => {                                   │
│    if (selectedTopic === '전체') {                                       │
│      return posts;                                                        │
│    }                                                                      │
│                                                                           │
│    return posts.filter(post => {                                         │
│      // 태그 기반 필터링                                                 │
│      if (post.tag === selectedTopic) return true;                        │
│                                                                           │
│      // 해시태그 기반 필터링                                             │
│      const hashtags = post.post_hashtags?.map(                           │
│        ph => ph.hashtags?.name                                           │
│      ) || [];                                                             │
│                                                                           │
│      return hashtags.includes(selectedTopic);                            │
│    });                                                                    │
│  }, [posts, selectedTopic]);                                             │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    필터링된 결과 표시                                     │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                                                                     │ │
│  │  📝 #react 게시글                     [최신순 ▼]  [#react ✕]       │ │
│  │                                                                     │ │
│  │  [전체] [#react ✓] [#javascript] [#python] ...                     │ │
│  │                                                                     │ │
│  │  ┌─────────────────────────────────────────────────────────────┐   │ │
│  │  │  React Hooks 완벽 정리                                       │   │ │
│  │  │  #react #hooks                                               │   │ │
│  │  └─────────────────────────────────────────────────────────────┘   │ │
│  │                                                                     │ │
│  │  ┌─────────────────────────────────────────────────────────────┐   │ │
│  │  │  React 상태 관리 비교                                        │   │ │
│  │  │  #react #redux #zustand                                      │   │ │
│  │  └─────────────────────────────────────────────────────────────┘   │ │
│  │                                                                     │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
```

---

## 4. 정렬 흐름

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              정렬 흐름                                               │
└─────────────────────────────────────────────────────────────────────────────────────┘

     사용자
        │
        │ 정렬 옵션 변경 (최신순 → 유용순)
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    정렬 처리                                              │
│                                                                           │
│  // 정렬 옵션 변경                                                        │
│  const handleSortChange = (newSort: 'latest' | 'useful') => {            │
│    setSortBy(newSort);                                                    │
│    setCurrentPage(1);  // 페이지 리셋                                    │
│  };                                                                       │
│                                                                           │
│  // 정렬된 게시글                                                         │
│  const sortedPosts = useMemo(() => {                                     │
│    const filtered = [...filteredPosts];                                  │
│                                                                           │
│    if (sortBy === 'latest') {                                            │
│      // 최신순: created_at 기준 내림차순                                 │
│      return filtered.sort((a, b) =>                                      │
│        new Date(b.created_at).getTime() -                                │
│        new Date(a.created_at).getTime()                                  │
│      );                                                                   │
│    }                                                                      │
│                                                                           │
│    if (sortBy === 'useful') {                                            │
│      // 유용순: 리뷰 점수 기준 내림차순                                  │
│      return filtered.sort((a, b) => {                                    │
│        const scoreA = a.reviews?.reduce(                                 │
│          (sum, r) => sum + r.score, 0                                    │
│        ) || 0;                                                            │
│        const scoreB = b.reviews?.reduce(                                 │
│          (sum, r) => sum + r.score, 0                                    │
│        ) || 0;                                                            │
│        return scoreB - scoreA;                                           │
│      });                                                                  │
│    }                                                                      │
│                                                                           │
│    return filtered;                                                       │
│  }, [filteredPosts, sortBy]);                                            │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
```

---

## 5. 페이지네이션 흐름

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                           페이지네이션 흐름                                          │
└─────────────────────────────────────────────────────────────────────────────────────┘

     사용자
        │
        │ [더 보기] 버튼 클릭 또는 스크롤
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    페이지네이션 로직                                      │
│                                                                           │
│  const POSTS_PER_PAGE = 10;                                              │
│                                                                           │
│  // 현재 페이지의 게시글                                                  │
│  const paginatedPosts = useMemo(() => {                                  │
│    const startIndex = 0;                                                  │
│    const endIndex = currentPage * POSTS_PER_PAGE;                        │
│    return sortedPosts.slice(startIndex, endIndex);                       │
│  }, [sortedPosts, currentPage]);                                         │
│                                                                           │
│  // 더 보기 버튼 표시 여부                                                │
│  const hasMore = paginatedPosts.length < sortedPosts.length;             │
│                                                                           │
│  // 더 보기 핸들러                                                        │
│  const handleLoadMore = () => {                                          │
│    setCurrentPage(prev => prev + 1);                                     │
│  };                                                                       │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    더 보기 버튼                                           │
│                                                                           │
│  {hasMore && (                                                           │
│    <button                                                                │
│      onClick={handleLoadMore}                                            │
│      className="w-full py-3 text-blue-600 hover:bg-blue-50"             │
│    >                                                                      │
│      더 보기 ({sortedPosts.length - paginatedPosts.length}개 남음)       │
│    </button>                                                              │
│  )}                                                                       │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
```

---

## 6. 게시판별 섹션 흐름

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                        게시판별 섹션 표시 흐름                                        │
└─────────────────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────────┐
│                    Timeline.tsx - 섹션 구분                               │
│                                                                           │
│  // 최신 게시글 (1주일 이내 또는 최대 10개)                              │
│  const latestPosts = useMemo(() => {                                     │
│    const oneWeekAgo = new Date();                                        │
│    oneWeekAgo.setDate(oneWeekAgo.getDate() - 7);                        │
│                                                                           │
│    return sortedPosts                                                     │
│      .filter(p => new Date(p.created_at) > oneWeekAgo)                   │
│      .slice(0, 10);                                                       │
│  }, [sortedPosts]);                                                       │
│                                                                           │
│  // 자유게시판 상위 5개                                                   │
│  const freeBoardPosts = useMemo(() => {                                  │
│    return sortedPosts                                                     │
│      .filter(p => p.board === '자유게시판')                               │
│      .slice(0, 5);                                                        │
│  }, [sortedPosts]);                                                       │
│                                                                           │
│  // 질문/답변 상위 5개                                                    │
│  const qnaPosts = useMemo(() => {                                        │
│    return sortedPosts                                                     │
│      .filter(p => p.board === '질문/답변')                                │
│      .slice(0, 5);                                                        │
│  }, [sortedPosts]);                                                       │
│                                                                           │
│  // 스터디 노트 상위 5개                                                  │
│  const studyNotePosts = useMemo(() => {                                  │
│    return sortedPosts                                                     │
│      .filter(p => p.board === '스터디 노트')                              │
│      .slice(0, 5);                                                        │
│  }, [sortedPosts]);                                                       │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    섹션별 UI                                              │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │  📝 최신 게시글                                                     │ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ...                          │ │
│  │  │ Post 1  │ │ Post 2  │ │ Post 3  │                              │ │
│  │  └─────────┘ └─────────┘ └─────────┘                              │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │  💬 자유게시판                                      [더 보기 →]    │ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ...                          │ │
│  │  └─────────┘ └─────────┘ └─────────┘                              │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │  ❓ 질문/답변                                       [더 보기 →]    │ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ...                          │ │
│  │  └─────────┘ └─────────┘ └─────────┘                              │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │  📚 스터디 노트                                     [더 보기 →]    │ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ...                          │ │
│  │  └─────────┘ └─────────┘ └─────────┘                              │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
```

---

## 7. 게시글 클릭 흐름

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                           게시글 클릭 흐름                                           │
└─────────────────────────────────────────────────────────────────────────────────────┘

     사용자
        │
        │ PostItem 클릭
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    PostItem.tsx                                           │
│                                                                           │
│  const handleClick = () => {                                             │
│    router.push(`/post/${post.id}`);                                      │
│  };                                                                       │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    /post/[id]/page.tsx                                    │
│                                                                           │
│  // 게시글 상세 페이지                                                    │
│  const { data: post } = await supabase                                   │
│    .from("posts")                                                         │
│    .select(`                                                              │
│      *,                                                                   │
│      profiles:user_id (username, school_name),                           │
│      post_hashtags (hashtags (name))                                     │
│    `)                                                                     │
│    .eq("id", id)                                                          │
│    .single();                                                             │
│                                                                           │
│  return (                                                                 │
│    <div>                                                                  │
│      <h1>{post.title}</h1>                                               │
│      <ReactMarkdown                                                       │
│        remarkPlugins={[remarkGfm, remarkMath]}                           │
│        rehypePlugins={[rehypeKatex]}                                     │
│      >                                                                    │
│        {post.content}                                                     │
│      </ReactMarkdown>                                                     │
│      <PostInteraction postId={post.id} />                                │
│      <CommentSection postId={post.id} />                                 │
│    </div>                                                                 │
│  );                                                                       │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
```

---

## 8. 컴포넌트 구조

```
Timeline.tsx
├── TopicFilter.tsx (해시태그 필터)
│   └── 해시태그 버튼들
├── 정렬 드롭다운
└── PostList.tsx (게시글 목록)
    └── PostItem.tsx (개별 게시글 카드)
        ├── 게시판 배지
        ├── 제목
        ├── 내용 미리보기
        ├── 해시태그 목록
        ├── 작성자 정보
        └── 상호작용 (좋아요, 댓글 수)
```

---

## 9. 관련 파일

| 파일 | 역할 |
|------|------|
| `src/component/Timeline.tsx` | 메인 타임라인 컴포넌트 |
| `src/component/PostList.tsx` | 게시글 목록 컴포넌트 |
| `src/component/PostItem.tsx` | 개별 게시글 카드 |
| `src/component/TopicFilter.tsx` | 해시태그 필터 |
| `src/app/post/[id]/page.tsx` | 게시글 상세 페이지 |
