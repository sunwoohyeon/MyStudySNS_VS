# 지식카드 생성 흐름도

## 1. 기능 개요

지식카드 생성은 게시글 내용을 AI가 분석하여 학습용 플래시카드를 자동 생성하는 기능입니다.

### 주요 특징

- **AI 자동 요약**: 게시글 핵심 내용 추출
- **카테고리 분류**: 과목/주제 자동 분류
- **키워드 추출**: 핵심 키워드 자동 태깅
- **카로셀 표시**: 홈 화면에서 카드 형태로 표시

---

## 2. 전체 흐름도

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                           지식카드 생성 전체 흐름                                    │
└─────────────────────────────────────────────────────────────────────────────────────┘

     사용자
        │
        │ 1. 게시글 상세 페이지에서 "지식카드 생성" 버튼 클릭
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    게시글 상세 페이지                                      │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                                                                     │ │
│  │  # React Hooks 완벽 정리                                            │ │
│  │                                                                     │ │
│  │  ## useState                                                        │ │
│  │  상태 관리를 위한 기본 훅...                                        │ │
│  │                                                                     │ │
│  │  ## useEffect                                                       │ │
│  │  사이드 이펙트 처리...                                              │ │
│  │                                                                     │ │
│  │  ## useContext                                                      │ │
│  │  전역 상태 관리...                                                  │ │
│  │                                                                     │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  👍 24  💬 5  📖 [지식카드 생성]                                         │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 2. 기존 카드 확인
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    중복 확인                                              │
│                                                                           │
│  // 이미 생성된 카드가 있는지 확인                                       │
│  const { data: existingCard } = await supabase                           │
│    .from("knowledge_cards")                                              │
│    .select("id")                                                          │
│    .eq("post_id", postId)                                                │
│    .single();                                                             │
│                                                                           │
│  if (existingCard) {                                                     │
│    return { error: "이미 지식카드가 생성되어 있습니다." };               │
│  }                                                                        │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 3. API 요청
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    POST /api/knowledge/generate                           │
│                                                                           │
│  요청 본문:                                                               │
│  {                                                                        │
│    "postId": 123                                                         │
│  }                                                                        │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 4. 게시글 내용 조회
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    게시글 데이터 조회                                     │
│                                                                           │
│  const { data: post } = await supabase                                   │
│    .from("posts")                                                         │
│    .select(`                                                              │
│      id,                                                                  │
│      title,                                                               │
│      content,                                                             │
│      image_url,                                                           │
│      tag                                                                  │
│    `)                                                                     │
│    .eq("id", postId)                                                     │
│    .single();                                                             │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 5. AI 분석 (Gemini 2.5 Flash)
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    Gemini 2.5 Flash 분석                                  │
│                                                                           │
│  // Flash 모델 사용 (빠른 응답 속도 우선)                                │
│  const model = genAI.getGenerativeModel({                                │
│    model: "gemini-2.5-flash-preview-05-20"                               │
│  });                                                                      │
│                                                                           │
│  const systemPrompt = `                                                  │
│  당신은 학습 콘텐츠를 분석하여 지식 카드를 생성하는 AI입니다.            │
│                                                                           │
│  다음 게시글을 분석하여 학습에 도움이 되는 지식 카드를 만들어주세요.     │
│                                                                           │
│  게시글 제목: ${post.title}                                              │
│  게시글 내용: ${post.content}                                            │
│  태그: ${post.tag}                                                        │
│                                                                           │
│  출력 JSON:                                                               │
│  {                                                                        │
│    "title": "호기심을 자극하는 제목 (원 제목과 다르게)",                 │
│    "summary": "마크다운 형식의 핵심 내용 요약 (최소 500자)",             │
│    "category": "Frontend | Backend | CS | Database | AI | Chemistry...",│
│    "keywords": ["키워드1", "키워드2", "키워드3", "키워드4", "키워드5"]   │
│  }                                                                        │
│                                                                           │
│  규칙:                                                                    │
│  1. title: 원래 제목과 다르게, 호기심을 자극하는 질문 또는 인사이트      │
│  2. summary: 핵심 개념을 쉽게 설명, 예시 포함, 마크다운 형식              │
│  3. category: 게시글 주제에 맞는 카테고리 1개                            │
│  4. keywords: 핵심 키워드 1~5개 (배열)                                   │
│  `;                                                                       │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 6. 이미지가 있는 경우 멀티모달 분석
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    멀티모달 분석 (이미지 포함 시)                          │
│                                                                           │
│  if (post.image_url) {                                                   │
│    // 이미지 다운로드 및 Base64 변환                                     │
│    const imageResponse = await fetch(post.image_url);                    │
│    const arrayBuffer = await imageResponse.arrayBuffer();                │
│    const base64 = Buffer.from(arrayBuffer).toString('base64');           │
│                                                                           │
│    // 멀티모달 분석                                                       │
│    const result = await model.generateContent([                          │
│      {                                                                    │
│        inlineData: {                                                      │
│          mimeType: 'image/jpeg',                                         │
│          data: base64                                                     │
│        }                                                                  │
│      },                                                                   │
│      { text: systemPrompt }                                              │
│    ]);                                                                    │
│  } else {                                                                │
│    // 텍스트만 분석                                                       │
│    const result = await model.generateContent(systemPrompt);             │
│  }                                                                        │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 7. 결과 파싱 및 검증
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    결과 파싱                                              │
│                                                                           │
│  // JSON 추출 (코드 블록 처리)                                           │
│  const text = result.response.text();                                    │
│                                                                           │
│  // ```json ... ``` 형식 처리                                            │
│  let jsonStr = text;                                                      │
│  const jsonMatch = text.match(/```json\s*([\s\S]*?)\s*```/);             │
│  if (jsonMatch) {                                                        │
│    jsonStr = jsonMatch[1];                                                │
│  } else {                                                                │
│    // ``` ... ``` 형식 처리                                              │
│    const codeMatch = text.match(/```\s*([\s\S]*?)\s*```/);               │
│    if (codeMatch) {                                                      │
│      jsonStr = codeMatch[1];                                              │
│    }                                                                      │
│  }                                                                        │
│                                                                           │
│  const parsed = JSON.parse(jsonStr);                                     │
│                                                                           │
│  // 데이터 검증 및 정규화                                                 │
│  const card = {                                                           │
│    title: parsed.title?.trim() || post.title,                            │
│    summary: parsed.summary?.trim() || "",                                │
│    category: parsed.category?.trim() || "기타",                          │
│    keywords: Array.isArray(parsed.keywords)                              │
│      ? [...new Set(parsed.keywords)].slice(0, 5)                         │
│      : []                                                                 │
│  };                                                                       │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 8. 데이터베이스 저장
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    knowledge_cards 테이블 저장                            │
│                                                                           │
│  const { data: newCard, error } = await supabase                         │
│    .from("knowledge_cards")                                              │
│    .insert({                                                              │
│      post_id: postId,                                                     │
│      user_id: user.id,                                                    │
│      title: card.title,                                                   │
│      summary: card.summary,                                               │
│      category: card.category,                                             │
│      keywords: card.keywords  // TEXT[] 배열 타입                        │
│    })                                                                     │
│    .select()                                                              │
│    .single();                                                             │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 9. 응답 반환
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    API 응답                                               │
│                                                                           │
│  return NextResponse.json({                                              │
│    message: "지식 카드가 생성되었습니다.",                               │
│    card: {                                                                │
│      id: newCard.id,                                                      │
│      post_id: newCard.post_id,                                           │
│      title: newCard.title,                                                │
│      summary: newCard.summary,                                            │
│      category: newCard.category,                                          │
│      keywords: newCard.keywords,                                          │
│      created_at: newCard.created_at                                      │
│    }                                                                      │
│  });                                                                      │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 10. 홈 화면에서 카드 표시
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    KnowledgeCards.tsx (홈 화면)                           │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │  📚 추천 지식카드                                              ◀ ▶ │ │
│  │                                                                     │ │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐               │ │
│  │  │ [Frontend]   │ │ [Backend]    │ │ [CS]         │               │ │
│  │  │              │ │              │ │              │               │ │
│  │  │ React Hooks  │ │ Node.js 비동 │ │ 운영체제     │               │ │
│  │  │ 왜 이렇게   │ │ 기 처리의    │ │ 프로세스 vs  │               │ │
│  │  │ 중요할까?   │ │ 비밀         │ │ 스레드       │               │ │
│  │  │              │ │              │ │              │               │ │
│  │  │ #react      │ │ #nodejs      │ │ #os          │               │ │
│  │  │ #hooks      │ │ #async       │ │ #process     │               │ │
│  │  └──────────────┘ └──────────────┘ └──────────────┘               │ │
│  │                                                                     │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  // 카드 클릭 시 모달 표시                                               │
│  const [selectedCard, setSelectedCard] = useState(null);                 │
│  const [showModal, setShowModal] = useState(false);                      │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 11. 카드 클릭 시 상세 모달
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    KnowledgeCardModal.tsx                                 │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                                                                [X] │ │
│  │  [Frontend]                                                        │ │
│  │                                                                     │ │
│  │  # React Hooks 왜 이렇게 중요할까?                                 │ │
│  │                                                                     │ │
│  │  ## 핵심 개념                                                       │ │
│  │                                                                     │ │
│  │  React Hooks는 함수형 컴포넌트에서 상태와 생명주기를               │ │
│  │  관리할 수 있게 해주는 기능입니다.                                  │ │
│  │                                                                     │ │
│  │  ### useState                                                       │ │
│  │  - 컴포넌트의 상태를 관리                                          │ │
│  │  - `const [state, setState] = useState(초기값)`                    │ │
│  │                                                                     │ │
│  │  ### useEffect                                                      │ │
│  │  - 사이드 이펙트 처리 (API 호출, 구독 등)                          │ │
│  │  - `useEffect(() => { ... }, [의존성])`                            │ │
│  │                                                                     │ │
│  │  ─────────────────────────────────────────────────────────────────│ │
│  │                                                                     │ │
│  │  #react  #hooks  #useState  #useEffect  #frontend                 │ │
│  │                                                                     │ │
│  │  [원본 게시글 보기]                              [삭제] (본인만)   │ │
│  │                                                                     │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 데이터 구조

```typescript
// 지식카드
interface KnowledgeCard {
  id: number;
  post_id: number;
  user_id: string;
  title: string;           // 호기심 자극 제목
  summary: string;         // 마크다운 형식 요약
  category: string;        // 카테고리
  keywords: string[];      // 키워드 배열
  created_at: string;
}

// 카테고리 목록
const categories = [
  "Frontend",
  "Backend",
  "CS",
  "Database",
  "AI",
  "Chemistry",
  "Physics",
  "Mathematics",
  "Biology",
  "기타"
];
```

---

## 4. 데이터베이스 스키마

```sql
-- knowledge_cards 테이블
CREATE TABLE public.knowledge_cards (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  post_id BIGINT NOT NULL REFERENCES posts(id) ON DELETE CASCADE,
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  title TEXT NOT NULL,
  summary TEXT NOT NULL,
  category TEXT NOT NULL,
  keywords TEXT[] DEFAULT '{}',
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(post_id)  -- 게시글당 1개의 카드만
);

-- RLS 정책
-- 모든 사용자가 읽기 가능
CREATE POLICY "Anyone can read" ON knowledge_cards
  FOR SELECT USING (true);

-- 인증된 사용자만 생성 가능
CREATE POLICY "Authenticated users can insert" ON knowledge_cards
  FOR INSERT WITH CHECK (auth.uid() IS NOT NULL);

-- 본인 카드만 삭제 가능
CREATE POLICY "Users can delete own cards" ON knowledge_cards
  FOR DELETE USING (auth.uid() = user_id);
```

---

## 5. 카드 표시 흐름

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                           카드 조회 및 표시 흐름                                     │
└─────────────────────────────────────────────────────────────────────────────────────┘

홈 페이지 로드
        │
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    KnowledgeCards.tsx                                     │
│                                                                           │
│  useEffect(() => {                                                       │
│    const fetchCards = async () => {                                      │
│      const { data } = await supabase                                     │
│        .from("knowledge_cards")                                          │
│        .select("*")                                                       │
│        .order("created_at", { ascending: false })                        │
│        .limit(10);                                                        │
│                                                                           │
│      setCards(data || []);                                               │
│    };                                                                     │
│                                                                           │
│    fetchCards();                                                          │
│  }, []);                                                                  │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    카로셀 렌더링                                          │
│                                                                           │
│  <div className="overflow-x-auto flex gap-4">                            │
│    {cards.map(card => (                                                  │
│      <div                                                                 │
│        key={card.id}                                                      │
│        className="min-w-[250px] bg-white rounded-lg shadow p-4"          │
│        onClick={() => handleCardClick(card)}                             │
│      >                                                                    │
│        <span className="badge">{card.category}</span>                    │
│        <h3 className="font-bold line-clamp-2">{card.title}</h3>          │
│        <p className="text-sm line-clamp-3">{card.summary}</p>            │
│        <div className="flex gap-1 flex-wrap">                            │
│          {card.keywords.slice(0, 5).map(kw => (                          │
│            <span className="tag">{kw}</span>                             │
│          ))}                                                              │
│        </div>                                                             │
│      </div>                                                               │
│    ))}                                                                    │
│  </div>                                                                   │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
```

---

## 6. 카드 삭제 흐름

```
사용자 (카드 소유자)
        │
        │ [삭제] 버튼 클릭
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    DELETE /api/knowledge/[id]                             │
│                                                                           │
│  // 권한 확인                                                             │
│  const { data: card } = await supabase                                   │
│    .from("knowledge_cards")                                              │
│    .select("user_id")                                                     │
│    .eq("id", cardId)                                                      │
│    .single();                                                             │
│                                                                           │
│  if (card.user_id !== user.id) {                                         │
│    return { error: "삭제 권한이 없습니다." };                            │
│  }                                                                        │
│                                                                           │
│  // 삭제 실행                                                             │
│  await supabase                                                           │
│    .from("knowledge_cards")                                              │
│    .delete()                                                              │
│    .eq("id", cardId);                                                     │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
```

---

## 7. 관련 파일

| 파일 | 역할 |
|------|------|
| `src/app/api/knowledge/generate/route.ts` | 카드 생성 API |
| `src/app/api/knowledge/[id]/route.ts` | 카드 삭제 API |
| `src/component/KnowledgeCards.tsx` | 카드 카로셀 컴포넌트 |
| `src/component/KnowledgeCardModal.tsx` | 카드 상세 모달 |
| `src/lib/knowledge_cards_schema.sql` | DB 스키마 |
