# 시간표 분석 흐름도

## 1. 기능 개요

시간표 분석은 시간표 이미지를 AI가 분석하여 수업 정보(과목명, 요일, 시간, 강의실)를 자동 추출하는 기능입니다.

### 주요 특징

- **다양한 형식 지원**: 24시간제, 12시간제, 교시제
- **스마트 변환**: 모든 형식을 24시간제로 자동 정규화
- **충돌 검사**: 기존 시간표와의 시간 겹침 감지
- **인라인 편집**: 추출된 정보 즉시 수정 가능

---

## 2. 전체 흐름도

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                           시간표 분석 전체 흐름                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘

     사용자
        │
        │ 1. 시간표 등록 페이지 접속
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    ImageScheduleForm.tsx                                  │
│                                                                           │
│  Step 1: 업로드                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                                                                     │ │
│  │     📤 시간표 이미지를 업로드하세요                                 │ │
│  │                                                                     │ │
│  │     ┌────────────────────────────────────────────────────────────┐ │ │
│  │     │                                                            │ │ │
│  │     │              [이미지 미리보기]                             │ │ │
│  │     │                                                            │ │ │
│  │     └────────────────────────────────────────────────────────────┘ │ │
│  │                                                                     │ │
│  │     1교시 시작 시간: [09:00 ▼]                                     │ │
│  │                                                                     │ │
│  │     [          분석 시작          ]                                │ │
│  │                                                                     │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 2. 이미지 선택 및 1교시 시간 설정
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    1교시 시작 시간 옵션                                   │
│                                                                           │
│  const firstPeriodOptions = [                                            │
│    "08:00",                                                               │
│    "08:30",                                                               │
│    "09:00",  // 기본값                                                   │
│    "09:30",                                                               │
│    "10:00"                                                                │
│  ];                                                                       │
│                                                                           │
│  // 교시제 시간표인 경우 이 시간을 기준으로 변환                         │
│  // 예: 1교시 = 09:00, 2교시 = 10:00 (50분 수업 + 10분 휴식)            │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 3. 분석 시작 버튼 클릭
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    POST /api/schedule/analyze                             │
│                                                                           │
│  요청 본문:                                                               │
│  {                                                                        │
│    "image": "base64_encoded_data...",                                    │
│    "mimeType": "image/jpeg",                                             │
│    "firstPeriodStart": "09:00"                                           │
│  }                                                                        │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    Gemini 2.5 Pro 분석                                    │
│                                                                           │
│  const systemPrompt = `                                                  │
│  당신은 시간표 이미지를 분석하는 AI입니다.                               │
│                                                                           │
│  시간표 형식을 파악하세요:                                                │
│  1. Y축 형식:                                                             │
│     - 24시간제: 9, 10, 11, 12, 13, 14...                                 │
│     - 12시간제: 9, 10, 11, 12, 1, 2... (12 이후 1로 반복)                │
│     - 교시제: 1교시, 2교시, 3교시...                                     │
│                                                                           │
│  2. X축: 요일 (월, 화, 수, 목, 금, 토, 일)                               │
│                                                                           │
│  3. 각 수업 블록에서 추출:                                                │
│     - title: 과목명                                                       │
│     - day_of_week: 요일                                                   │
│     - start_time: 시작 시간 (HH:MM, 24시간제)                            │
│     - end_time: 종료 시간 (HH:MM, 24시간제)                              │
│     - location: 강의실 (없으면 null)                                     │
│                                                                           │
│  출력 JSON:                                                               │
│  {                                                                        │
│    "schedules": [...],                                                    │
│    "total_count": 12,                                                    │
│    "y_axis_format": "24hour" | "12hour" | "period",                      │
│    "analysis_notes": "분석 메모"                                         │
│  }                                                                        │
│  `;                                                                       │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    시간 형식 자동 감지 및 변환                             │
│                                                                           │
│  // 12시간제 → 24시간제 변환                                             │
│  const convertTo24Hour = (hour: number, isPM: boolean): number => {      │
│    if (hour === 12) return isPM ? 12 : 0;                                │
│    return isPM ? hour + 12 : hour;                                        │
│  };                                                                       │
│                                                                           │
│  // 교시제 → 24시간제 변환                                               │
│  const periodToTime = (period: number, firstStart: string): string => { │
│    const [startHour, startMin] = firstStart.split(':').map(Number);      │
│    const totalMinutes = startHour * 60 + startMin + (period - 1) * 60;  │
│    const hour = Math.floor(totalMinutes / 60);                           │
│    const min = totalMinutes % 60;                                         │
│    return `${hour.toString().padStart(2, '0')}:${min...}`;              │
│  };                                                                       │
│                                                                           │
│  예시:                                                                    │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │  원본 (12시간제)  │  변환 (24시간제)                                │ │
│  │  9:00 AM         │  09:00                                           │ │
│  │  12:00 PM        │  12:00                                           │ │
│  │  1:00 PM         │  13:00                                           │ │
│  │  3:30 PM         │  15:30                                           │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │  원본 (교시제)   │  변환 (24시간제, 1교시=09:00)                    │ │
│  │  1교시           │  09:00 ~ 09:50                                   │ │
│  │  2교시           │  10:00 ~ 10:50                                   │ │
│  │  3교시           │  11:00 ~ 11:50                                   │ │
│  │  4교시           │  12:00 ~ 12:50                                   │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    데이터 검증 및 정규화                                  │
│                                                                           │
│  // 필수 필드 검증                                                        │
│  const validateSchedule = (item: ScheduleItem) => {                      │
│    if (!item.title) throw new Error("과목명 필수");                       │
│    if (!item.day_of_week) throw new Error("요일 필수");                   │
│    if (!item.start_time) throw new Error("시작 시간 필수");              │
│    if (!item.end_time) throw new Error("종료 시간 필수");                │
│  };                                                                       │
│                                                                           │
│  // 요일 검증                                                             │
│  const validDays = ['월', '화', '수', '목', '금', '토', '일'];           │
│  if (!validDays.includes(item.day_of_week)) {                            │
│    throw new Error("유효하지 않은 요일");                                │
│  }                                                                        │
│                                                                           │
│  // 시간 형식 정규화 (9:00 → 09:00)                                      │
│  const normalizeTime = (time: string): string => {                       │
│    const [h, m] = time.split(':');                                        │
│    return `${h.padStart(2, '0')}:${m.padStart(2, '0')}`;                │
│  };                                                                       │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 4. 분석 결과 반환
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    Step 2: 미리보기 (Preview)                             │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │  📅 추출된 시간표 (12개)                 [전체 선택] [전체 해제]   │ │
│  │                                                                     │ │
│  │  ☑ │ 과목명        │ 요일 │ 시작   │ 종료   │ 강의실  │ 상태     │ │
│  │  ──┼───────────────┼──────┼────────┼────────┼─────────┼──────────│ │
│  │  ☑ │ 웹프로그래밍  │ 월   │ 09:00  │ 10:30  │ IT101   │ ✓        │ │
│  │  ☑ │ 자료구조      │ 화   │ 10:00  │ 12:00  │ IT203   │ ⚠ 충돌   │ │
│  │  ☑ │ 알고리즘      │ 수   │ 14:00  │ 16:00  │ IT305   │ ✓        │ │
│  │  ☐ │ 데이터베이스  │ 목   │ 09:00  │ 11:00  │ IT102   │ ✓        │ │
│  │  ...                                                                │ │
│  │                                                                     │ │
│  │  [행 편집]  [행 삭제]                                              │ │
│  │                                                                     │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  ⚠ 기존 시간표와 충돌하는 항목이 2개 있습니다.                          │
│  ☐ 기존 시간표 삭제 후 새로 등록                                        │
│                                                                           │
│  [← 다시 분석]                                     [저장하기 →]         │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    충돌 검사 로직                                         │
│                                                                           │
│  // 시간 겹침 검사                                                        │
│  const checkTimeOverlap = (                                              │
│    start1: string, end1: string,                                         │
│    start2: string, end2: string                                          │
│  ): 'full' | 'partial' | null => {                                       │
│    const toMinutes = (t: string) => {                                    │
│      const [h, m] = t.split(':').map(Number);                            │
│      return h * 60 + m;                                                   │
│    };                                                                     │
│                                                                           │
│    const [s1, e1] = [toMinutes(start1), toMinutes(end1)];               │
│    const [s2, e2] = [toMinutes(start2), toMinutes(end2)];               │
│                                                                           │
│    if (s1 >= s2 && e1 <= e2) return 'full';    // 완전 포함             │
│    if (s1 < e2 && e1 > s2) return 'partial';   // 부분 겹침             │
│    return null;                                  // 겹침 없음            │
│  };                                                                       │
│                                                                           │
│  // 기존 시간표와 비교                                                    │
│  existingSchedules.forEach(existing => {                                 │
│    if (existing.day_of_week === newItem.day_of_week) {                  │
│      const overlap = checkTimeOverlap(                                   │
│        newItem.start_time, newItem.end_time,                             │
│        existing.start_time, existing.end_time                            │
│      );                                                                   │
│      if (overlap) {                                                      │
│        newItem.hasConflict = true;                                       │
│        newItem.conflictType = overlap;                                   │
│      }                                                                    │
│    }                                                                      │
│  });                                                                      │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 5. 인라인 편집 (선택적)
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    인라인 편집 모드                                       │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │  ☑ │ [웹프로그래밍  ]│ [월▼]│ [09:00]│ [10:30]│ [IT101  ]│ [저장] │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
│  // 편집 상태 관리                                                        │
│  const [editingId, setEditingId] = useState<string | null>(null);        │
│  const [editedItem, setEditedItem] = useState<ScheduleItem | null>(null);│
│                                                                           │
│  // 편집 시작                                                             │
│  const handleEdit = (item: ScheduleItem) => {                            │
│    setEditingId(item.id);                                                │
│    setEditedItem({ ...item });                                           │
│  };                                                                       │
│                                                                           │
│  // 편집 저장                                                             │
│  const handleSaveEdit = () => {                                          │
│    setSchedules(prev => prev.map(s =>                                    │
│      s.id === editingId ? editedItem : s                                 │
│    ));                                                                    │
│    setEditingId(null);                                                   │
│  };                                                                       │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 6. 저장하기 클릭
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    Step 3: 저장 (Saving)                                  │
│                                                                           │
│  // 선택된 항목만 필터링                                                  │
│  const selectedItems = schedules.filter(s => s.selected);                │
│                                                                           │
│  // 기존 시간표 삭제 (옵션 선택 시)                                       │
│  if (deleteExisting) {                                                   │
│    await supabase                                                         │
│      .from('schedules')                                                   │
│      .delete()                                                            │
│      .eq('user_id', user.id);                                            │
│  }                                                                        │
│                                                                           │
│  // 새 시간표 저장                                                        │
│  const { error } = await supabase                                        │
│    .from('schedules')                                                     │
│    .insert(                                                               │
│      selectedItems.map(item => ({                                        │
│        user_id: user.id,                                                  │
│        title: item.title,                                                 │
│        day_of_week: item.day_of_week,                                    │
│        start_time: item.start_time,                                      │
│        end_time: item.end_time,                                          │
│        location: item.location                                            │
│      }))                                                                  │
│    );                                                                     │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
        │
        │ 7. 저장 완료
        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                    홈 화면 - 시간표 위젯                                  │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │  📅 내 시간표                              [오늘 ▼] [전체 보기]   │ │
│  │                                                                     │ │
│  │  ┌────┬────┬────┬────┬────┬────┐                                  │ │
│  │  │    │ 월 │ 화 │ 수 │ 목 │ 금 │                                  │ │
│  │  ├────┼────┼────┼────┼────┼────┤                                  │ │
│  │  │ 9  │ 웹 │    │    │ DB │    │                                  │ │
│  │  │ 10 │ 프 │ 자 │    │    │    │                                  │ │
│  │  │ 11 │    │ 료 │    │    │    │                                  │ │
│  │  │ 12 │    │    │    │    │    │                                  │ │
│  │  │ ...│    │    │    │    │    │                                  │ │
│  │  └────┴────┴────┴────┴────┴────┘                                  │ │
│  │                                                                     │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 데이터 구조

```typescript
// 추출된 시간표 항목
interface ScheduleItem {
  id: string;              // 고유 ID
  title: string;           // 과목명
  day_of_week: string;     // 요일 (월~일)
  start_time: string;      // 시작 시간 (HH:MM)
  end_time: string;        // 종료 시간 (HH:MM)
  location: string | null; // 강의실

  // UI 상태
  selected: boolean;       // 선택 여부
  hasConflict: boolean;    // 충돌 여부
  conflictType?: 'full' | 'partial';
}

// API 응답
interface AnalysisResponse {
  success: boolean;
  data: {
    schedules: ScheduleItem[];
    total_count: number;
    y_axis_format: '24hour' | '12hour' | 'period';
    analysis_notes: string;
  };
  message: string;
}
```

---

## 4. 에러 처리

```
에러 종류:
├── 이미지 형식 오류: "지원하지 않는 이미지 형식입니다."
├── 이미지 크기 오류: "이미지 크기는 10MB 이하여야 합니다."
├── 분석 실패: "시간표를 인식할 수 없습니다."
├── 형식 감지 실패: "시간표 형식을 파악할 수 없습니다."
└── 저장 실패: "시간표 저장에 실패했습니다."
```

---

## 5. 관련 파일

| 파일 | 역할 |
|------|------|
| `src/component/ImageScheduleForm.tsx` | 메인 UI 컴포넌트 |
| `src/app/api/schedule/analyze/route.ts` | AI 분석 API |
| `src/component/ScheduleWidget.tsx` | 시간표 표시 위젯 |
| `src/component/WeeklyScheduleGrid.tsx` | 주간 시간표 그리드 |
